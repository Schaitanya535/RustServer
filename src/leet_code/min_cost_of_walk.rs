use super::data_structures::weighted_graph;

pub fn minimum_cost(n: i32, edges: Vec<Vec<i32>>, query: Vec<Vec<i32>>) -> Vec<i32> {
    let mut graph = weighted_graph::WeightedGraph::create_from_weighted_matrix(edges);

    vec![]
}
